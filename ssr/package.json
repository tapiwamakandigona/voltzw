{
  "name": "zesa-ssr",
  "version": "1.0.0",
  "private": true,
  "description": "SSR wrapper for zesa.tapiwa.me: real security headers, correct 404 status, cache split",
  "scripts": {
    "build:bundle": "esbuild server/index.mjs --bundle --platform=node --target=node22 --format=cjs --outfile=dist-bundle/server.js && rm -rf dist-bundle/site && cp -r site dist-bundle/site",
    "test": "node test/server.test.mjs"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
